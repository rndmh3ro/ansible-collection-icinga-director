- hosts: localhost
  gather_facts: false
  vars:
    icinga_url: "http://icinga"
    icinga_username: "icingaadmin"
    icinga_password: "icinga"
  tasks:
    - include: "{{ item }}"
      with_fileglob:
        - ./icinga_*.yml
